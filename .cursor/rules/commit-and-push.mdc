---
description: 完成代码修改后主动提交并推送到远程，不要等用户提醒
alwaysApply: false
---
# 提交与推送

- 在本仓库完成代码修改后，**主动**执行提交并推送，不要等用户说「上传」「提交」「push」再去做。
- 流程：`git status` 确认有变更 → `git add` 相关文件 → `git commit -m "简短说明"` → `git push`（若有远程）。
- 提交信息用英文、简洁（如 `feat: ...` / `fix: ...`）。
- 若用户明确说「先别提交」或当前仅讨论/看代码未改文件，则可不提交。
